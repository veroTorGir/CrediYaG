spring:
  datasource:
    url: jdbc:postgresql://localhost:8080/CrediYaG
    password: admin
    username: admin
  jpa:
    hibernate:
      ddl-auto:update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgresSQL9Dialect
    show-sql: true
